# Required for HTTPS mode (HTTP_ONLY=0)
DOMAIN=lampac.example.com

# Recommended: Let's Encrypt contact email
ACME_EMAIL=admin@example.com

# Optional: set to 1 for temporary HTTP-only bootstrap mode
HTTP_ONLY=0
