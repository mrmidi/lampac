{
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	encode zstd gzip

	# Lampac stays internal; Caddy is the public edge.
	reverse_proxy lampac:9118 {
		health_uri /version
		health_interval 30s
		health_timeout 5s
		header_up X-Forwarded-Proto {scheme}
		header_up X-Forwarded-Host {host}
	}
}
